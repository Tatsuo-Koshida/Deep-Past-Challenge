# Data directory

This folder is for locally downloaded datasets (e.g., Kaggle competition files).

- Downloaded files are ignored by git via `.gitignore`.
- Keep per-dataset notes in `data/**/README.md` so the repo remains reproducible.
- Curated (hand-improved) datasets intended to be tracked live under `data/curated/`.
